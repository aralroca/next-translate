function templateWithLoader(
  rawCode,
  { i18nFile, arePagesInsideSrc, page, prefix, loader } = {}
) {
  console.log('@todo templateWithLoader', loader)

  return rawCode // @todo implement
}

export default templateWithLoader
